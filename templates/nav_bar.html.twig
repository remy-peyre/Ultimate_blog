<nav class="navbar navbar-default ">
    <div id="navbar" class="collapse navbar-collapse pull-right">
        <ul class="nav navbar-nav">
            {% if app.request.get('_route') not in ['homepage', 'entries'] %}
                {#<li><a href="{{ path("homepage") }}">Home</a></li>
                <li><a href="{{ path("register") }}">Register</a></li>
                <li class="active"><a href="{{path("login")}}">Login</a></li>#}
            {% endif %}
            {% if app.user %}
                <li class="active"><a href="{{path("homepage")}}">Home</a></li>

                {% if is_granted('ROLE_ADMIN') %}
                    <li><a href="{{ path("admin") }}">Admin</a></li>
                {% endif %} 
                <li><a href="{{path("profil")}}">Profil</a></li>
                {% if is_granted('ROLE_ADMIN') or is_granted('ROLE_BLOGGER') %}
                    <li><a href="{{ path("create-post") }}">Create an article</a></li>
                {% endif %}
                <li><a href="{{path("logout")}}">Logout</a></li>
            {% else %}
                <li><a href="{{ path("homepage") }}">Home</a></li>
                <li><a href="{{ path("register") }}">Register</a></li>
                <li class="active"><a href="{{path("login")}}">Login</a></li>
            {% endif %}
        </ul>
    </div>
</nav>